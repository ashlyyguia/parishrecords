# Copy to .env and set a long random OCR_SERVICE_KEY (must match the
# backend's OCR_SERVICE_KEY). Requests to /v1/grid must send it in the
# X-OCR-Service-Key header. Other OCR_* settings are optional.
OCR_SERVICE_KEY=change-me-to-a-long-random-string
