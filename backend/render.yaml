services:
  - type: web
    name: parish-record-api
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node src/server.js"
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
