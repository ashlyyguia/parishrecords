# Parish Record Backend Configuration

# Server Configuration
PORT=3000
NODE_ENV=development

# Cassandra Database
CASSANDRA_HOST=localhost
CASSANDRA_PORT=9042
CASSANDRA_DATACENTER=datacenter1
CASSANDRA_KEYSPACE=parish_records

# Optional: Cassandra Authentication (if enabled)
# CASSANDRA_USERNAME=cassandra
# CASSANDRA_PASSWORD=cassandra

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=gonzagaprince919@gmail.com
SMTP_PASS=xnpriiynyzrpuozv
SMTP_FROM="Parish Record <gonzagaprince919@gmail.com>"
